"use strict";

/*global describe:false,it:false*/

var assert = require('assert');

describe('core', function () {
    it('must respond', function (done) {
        assert.ok(false);
        done();
    });
});
